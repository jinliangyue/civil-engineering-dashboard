# ============================================================
# DEPLOYMENT dependency specification — Streamlit Cloud
# ============================================================
# Active: 2026-09-03 (P0.10.1) — replaces the legacy unpinned list.
# requirements.txt is the ONLY install spec Community Cloud reads
# (requirements-deploy.txt documents the same matrix; runtime.txt
# does NOT control the Cloud Python version).
#
# Python 3.12 verified matrix — identical to requirements-research.txt:
# every pin has a cp312 linux wheel on PyPI (numpy/scipy/scikit-learn
# manylinux2014, pandas/torch manylinux_2_28, xgboost/prophet py3-none,
# cmdstanpy/stanio/holidays py3-none-any). None of numpy 2.0.2 /
# scipy 1.13.1 / scikit-learn 1.6.1 / torch 2.8.0 has a cp314 wheel, so
# this app CANNOT build on Python 3.14 — the Cloud app must run 3.12.
#
# Pin policy (carried over from requirements-deploy.txt):
# - Pinning matches the verified research matrix to minimize live-demo
#   metric drift; it does NOT imply reproduction of formal results
#   (the app re-trains per session — see docs/ENVIRONMENT.md).
# - streamlit deliberately unpinned: Streamlit Cloud manages its own
#   Streamlit runtime.
# - akshare is fetch-only and lazily imported; the app reads the
#   committed CSV. Kept unpinned for optional data refresh.
# ============================================================

# --- pinned core (10) — same matrix as research, installable on 3.12 ---
pandas==2.3.3
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
xgboost==2.1.4
torch==2.8.0
prophet==1.3.0
cmdstanpy==1.3.0
stanio==0.5.1
holidays==0.83

# --- app runtime / platform-controlled ---
streamlit

# --- charting (pinned to the version verified in the AppTest venv) ---
plotly==7.0.0

# --- optional / fetch-only ---
akshare

# --- build constraint: keep pkg_resources available ---
setuptools<81
